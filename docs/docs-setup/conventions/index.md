# docs-setup conventions

Prescriptive rules scoped to the `docs-setup` skill and its distributed assets.

- [Byte-exact assets contract](/docs-setup/conventions/byte-exact-assets.md) - editing rules for `skills/docs-setup/assets/`, intentional placeholders, the `pnpm docs:validate` substitution target, and the `claude/` → `.claude/` install rename
