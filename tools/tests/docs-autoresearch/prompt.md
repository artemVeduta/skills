This repository has an OKF v0.1 documentation bundle under `docs/`, including a `references/` area for external material mirrored as first-class concepts.

Use the docs-autoresearch skill to research **Semantic Versioning precedence and version-comparison rules** and prepare to file the result as one curated `Reference` in the docs bundle (the default Reference-enrichment mode). Restrict yourself to public, reputable sources.

Do your research, then present your complete filing plan in one message — the concept path, its frontmatter, the section outline, the contradictions and low-confidence claims you found, the index change, and the one lifecycle entry — and stop. Change nothing until I explicitly approve.
