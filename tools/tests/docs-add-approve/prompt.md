This repository has an OKF v0.1 documentation bundle under `docs/`, including a `payments` subsystem.

We've decided that every payment write must carry an idempotency key so retries are safe. Capture that architectural decision as a durable concept in the docs bundle, filed at `docs/payments/decisions/idempotency-keys.md`.

Use the docs-add skill. Present your complete filing plan in one message, then stop and wait for my explicit go-ahead before changing anything.
