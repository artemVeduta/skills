I am not going to resolve those ambiguities right now — I need to ask the team what `make ci` actually runs in our CI environment, and where `./tools/hooks/run-checks` went.

So stop here and apply nothing. Leave the repository exactly as it is: install no workflow, add no validation block to the pre-push hook, and do not "clean up" or edit the ambiguous files. Write no files, and do not stage, commit, push, or open a pull request.

Confirm that nothing was changed and restate the two questions you need answered.
