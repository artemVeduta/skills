Yes — split it into the two concepts as you proposed. The old `notes/payments-guide.md` was just a scratch note and is not a useful entry point on its own, so once the content lives in the bundle, delete it. Nothing else links to it.

Do the whole thing in the working tree; do not stage, commit, push, or open a pull request.
