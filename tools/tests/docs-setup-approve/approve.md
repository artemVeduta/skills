Yes — that plan is exactly right. Apply it now.

Install the machinery as planned: copy the verbatim validator and its test, seed the policy, OKF reference, and bundle skeleton, add the two package.json scripts, splice the marked Documentation router into AGENTS.md (preserving the existing guidance), and write the exact CLAUDE.md shim. Then run the validator to confirm it exits 0. Do not stage, commit, push, or open a pull request.
