This repository has an OKF v0.1 documentation bundle under `docs/`, including a `specs/` area of versioned contracts and a `references/` area for external material.

Use the docs-autoresearch skill in **Specification-resolution mode**. The target Specification is `docs/specs/semver-precedence-policy.md`; the question to resolve is its open question — whether a pre-release version ranks lower than its associated normal version. Research it against public, reputable sources (the Semantic Versioning specification is the primary source).

Do your research, then present your complete filing plan in one message — the exact edit to the target Specification, whether any Reference is warranted, the index and lifecycle changes, and the contradictions or low-confidence claims you found — and stop. Change nothing until I explicitly approve. Persist summaries and citations only, never raw source bodies. Do not stage, commit, push, or open a pull request.
