This repository has an OKF v0.1 documentation bundle under `docs/` and a repository-owned `research/` directory (outside the bundle) for temporary pre-work notes.

Use the docs-autoresearch skill in **pre-work reconnaissance mode** to research **Semantic Versioning precedence and version-comparison rules** against public, reputable sources. Today's date is 2026-07-24; write the reconnaissance brief to `research/2026-07-24-semver-precedence-recon.md`.

Reconnaissance mode writes only that one dated brief outside the OKF bundle — no OKF index, log, or lifecycle ceremony inside `docs/`. Present what you intend to write and stop; write nothing until I approve. Persist summaries and citations only, never raw source bodies. Do not stage, commit, push, or open a pull request.
