Upgrade the OKF (Open Knowledge Format) v0.1 documentation TOOLING in THIS repository using the docs-setup skill.

This repository was configured by an EARLIER docs-setup run: it already has a `docs/` bundle, the `docs:validate` package scripts, a marked Documentation router in `AGENTS.md`, and the `CLAUDE.md` shim. Do NOT ask any questions; all inputs are below:

- Target repo root: the current working directory
- Project name: FixtureProj
- Package manager: npm

Run the mandatory read-only audits and recompute the actual state from the repository itself — do NOT read or write any suite-version or state file. Classify the state and present your COMPLETE plan in one message: the classification, the worktree state, and every file you would create, replace, preserve, or leave as a no-op, plus every ambiguity. Treat any managed file that DIFFERS from the canonical machinery as customized-until-reviewed — never blindly overwrite it; propose the review explicitly. Then STOP and wait for my explicit go-ahead. Do NOT write, copy, or substitute anything, and do NOT stage, commit, push, or open a pull request until I say yes.
