Upgrade the OKF (Open Knowledge Format) v0.1 documentation TOOLING in THIS repository using the docs-setup skill.

This repository was configured by an EARLIER docs-setup run and has since accumulated some UNCOMMITTED work in the working tree. Do NOT ask any questions; all inputs are below:

- Target repo root: the current working directory
- Project name: FixtureProj
- Package manager: npm

Run the mandatory read-only audits and recompute the actual state from the repository itself — do NOT read or write any suite-version or state file. Inspect the Git worktree state. Classify the state and present your COMPLETE plan in one message: the classification, the worktree state (clean or dirty), and every file you would create, replace, preserve, or leave as a no-op, plus every ambiguity. Then STOP and wait. Do NOT write, copy, or substitute anything, and do NOT stage, commit, push, or open a pull request. An upgrade is clean-worktree by default: if the worktree is dirty, say so plainly and require my explicit approval before you would proceed.
