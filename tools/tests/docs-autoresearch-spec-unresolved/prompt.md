This repository has an OKF v0.1 documentation bundle under `docs/`, including a `specs/` area of versioned contracts.

Use the docs-autoresearch skill in **Specification-resolution mode**. The target Specification is `docs/specs/semver-precedence-policy.md`; the question to resolve is its open question. Research it against public, reputable sources.

Specification-resolution mode changes the target only when the evidence actually resolves or materially narrows the question. If the evidence does not resolve it, leave the target Specification exactly as it is and tell me what remains open. Persist summaries and citations only, never raw source bodies. Do not stage, commit, push, or open a pull request.
