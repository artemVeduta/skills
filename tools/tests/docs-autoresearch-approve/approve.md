Yes — that plan is exactly right. Apply it now.

File the one curated Reference at the planned path through docs-add, add the one references-index entry and the one log entry as planned, then validate the bundle and read the concept back. Persist summaries and citations only — no raw source bodies. Do not stage, commit, push, or open a pull request.
