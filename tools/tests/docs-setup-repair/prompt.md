Repair the OKF (Open Knowledge Format) v0.1 documentation TOOLING in THIS repository using the docs-setup skill.

This repository was configured by an earlier docs-setup run but its machinery is incomplete. Do NOT ask any questions; all inputs are below:

- Target repo root: the current working directory
- Project name: FixtureProj
- Package manager: npm

Run the mandatory read-only audits and recompute the actual state from the repository itself — do NOT read or write any suite-version or state file. Classify the state and present your COMPLETE plan in one message: the classification, the worktree state, and every file you would create, replace, preserve, or leave as a no-op, plus every ambiguity. Missing managed files are proposed for creation; managed files that are already current are no-ops. Leave any existing `docs/specifications/` content untouched and do NOT rename it. Then STOP and wait for my explicit go-ahead. Do NOT write, copy, or substitute anything, and do NOT stage, commit, push, or open a pull request until I say yes.
