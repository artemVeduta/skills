Yes — apply the plan now.

Create the missing `scripts/validate-docs.test.mjs` byte-identical to the canonical machinery, add the two `docs:validate` package scripts, and leave every already-current file (the validator, the bundle, the policy, the OKF reference, the router, the shim) as a no-op. Do not touch the existing `docs/specifications/` content and do not rename it. Do not stage, commit, push, or open a pull request.
