This repository has an OKF v0.1 documentation bundle under `docs/`, including a `references/` area for external material mirrored as first-class concepts.

Use the docs-autoresearch skill to research **Semantic Versioning precedence and version-comparison rules** and file the result as one curated `Reference` at `docs/references/semver-precedence.md` (the default Reference-enrichment mode). Restrict yourself to public, reputable sources.

Before doing anything else, check the target path and proceed according to the skill's own filing rules. Never overwrite unrelated existing content. Do not stage, commit, push, or open a pull request.
