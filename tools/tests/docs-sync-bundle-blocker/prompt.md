This repository has an OKF v0.1 documentation bundle under `docs/`, including a `payments` subsystem.

Use the docs-sync skill in bundle-wide reconciliation mode. Audit and reconcile the complete bundle against the current source so the whole bundle is up to date.

Work in the working tree only. Do not stage, commit, push, or open a pull request.
